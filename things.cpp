#include "things.h"
#include <iostream>
/*
using namespace std;

Food::Food(const string name) : name(name) {};

ostream & operator<<(ostream &os, const Food &f) {
    os << f.name;
    return os;
};


Event::Event(const string name, int data) : name(name), data(data) {};

ostream &operator<<(ostream &os, const Event &e) {
    os << "(" << e.name << ", " << e.data << ")";
    return os;
}
*/
